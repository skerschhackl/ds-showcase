# Prompt Template: Settings Page

Generate a settings page using only approved design system components.

## Inputs

- Product area:
- Primary user:
- Settings groups:
- Risks or constraints:

## Instructions

Use `Button`, `Input`, `Select`, `Textarea`, `Badge`, `Alert`, `Tabs`, and `Table` when they fit the workflow. Use tokenized spacing, color, focus, and surface styles. Prefer direct operational copy over marketing language.

## Output

Return a short screen plan, the component list, and the final UI composition.
